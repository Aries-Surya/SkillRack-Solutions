#include <stdio.h>

int main() {
    int N;

    // Read the integer input
    scanf("%d", &N);

    // Calculate the square of the integer
    int square = N * N;

    // Print the result
    printf("%d\n", square);

    return 0;
}
