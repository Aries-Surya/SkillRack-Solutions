#include <stdio.h>

int main() {
    int A, B, C, D;

    // Read four numbers from input
    scanf("%d %d %d %d", &A, &B, &C, &D);

    // Compute the product of B and C
    int product = B * C;

    // Print the product
    printf("%d\n", product);

    return 0;
}
