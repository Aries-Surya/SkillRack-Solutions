#include <stdio.h>

int main() {
    int A, B, C;
    
    // Read three numbers A, B, and C from input
    scanf("%d %d %d", &A, &B, &C);
    
    // Print the numbers in reverse order
    printf("%d %d %d\n", C, B, A);
    
    return 0;
}
