#include <stdio.h>

int main() {
    int A;
    
    // Read number A from input
    scanf("%d", &A);
    
    // Compute A multiplied by 100
    int result = A * 100;
    
    // Print the result
    printf("%d\n", result);
    
    return 0;
}
