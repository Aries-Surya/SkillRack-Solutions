#include <stdio.h>

int main() {
    int A, B, C;
    scanf("%d %d %d", &A, &B, &C);

    // Calculate the sum of the first two integers
    int sum = A + B;

    // Multiply the sum with the third integer
    int result = sum * C;

    // Print the result
    printf("%d\n", result);

    return 0;
}
#error
