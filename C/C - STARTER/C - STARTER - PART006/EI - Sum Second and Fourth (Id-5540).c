#include <stdio.h>

int main() {
    int a, b, c, d;
    scanf("%d %d %d %d", &a, &b, &c, &d); // Read four integers
    int sum = b + d; // Compute sum of second and fourth integers
    printf("%d\n", sum); // Output the sum
    return 0;
}
